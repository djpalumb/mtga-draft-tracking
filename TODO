- Listen to logfile for new lines
    - Parse to find start of draft (and type of draft)
- Display cards and winrates for most recent draft choice
- Build info for last full draft 
    - Allow user to page to previous packs (display pack on top)
    - Allow user to see known missing if a wheel pack (based on draft type)